namespace Clock.Events
{
    public enum EventRepetitionType
    { 
        Every_Second,
        Every_Minute,
        Every_Hour,
        Every_DayPhase,
        Every_Day,
        Every_Month,
        Every_Year,
    }
}